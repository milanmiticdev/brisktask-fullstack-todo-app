{
	"name": "ts-setup",
	"version": "1.0.0",
	"description": "",
	"main": "./backend/src/app.ts",
	"scripts": {
		"build-server": "npm install --prefix backend && npm run build --prefix backend",
		"build-client": "npm install --prefix frontend && npm run build --prefix frontend",
		"build": "npm run build-server && npm run build-client",
		"start-server": "npm run start --prefix backend",
		"start": "npm run start-server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC"
}
